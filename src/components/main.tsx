import React from 'react';
import { Routes, Route } from 'react-router-dom';

const Main = () => {
  return (
    <main className="main">
      <Routes>
        <Route path="/" element={null} />
        <Route path="/" element={null} />
        <Route path="/" element={null} />
      </Routes>
    </main>
  );
};

export default Main;
